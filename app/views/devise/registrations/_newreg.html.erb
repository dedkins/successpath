
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>

<div class="inputbox">
	<div class="field">Email</div>
	<%= f.email_field :email, :autofocus => true, :placeholder => "Email", :class => 'inputfield' %>
	<div class="field">Password</div>
	<%= f.password_field :password, :placeholder => "Password", :class => 'inputfield' %>
	<div class="field">Confirm Password</div>
	<%= f.password_field :password_confirmation, :placeholder => "Confirm Password", :class => 'inputfield', :style => 'margin-bottom:20px' %>

	<%= f.submit "Go!", :class => "button-link", :style => 'font-size:1.1em' %>
	<%= link_to 'Sign In Instead', new_member_session_path, :class => 'button-link' %>
</div>

<% end %>